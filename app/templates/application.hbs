{{page-title "Tds"}}

{{!-- The following component displays Ember's default welcome message. --}}
{{!-- Feel free to remove this! --}}
<div class="container">
  <div class="card">
    <LinkTo class="card-header" @route="contacts">Contacts</LinkTo>
    <LinkTo class="card-header" @route="categories">Categories</LinkTo>
    <div class="card-body">
      {{outlet}}
    </div>
  </div>
</div>